{
  "name": "solution",
  "version": "1.0.0",
  "description": "It is going to be responsible for tasks such as minifying the css and compiling SASS into CSS so that developers don't have to keep running SASS watches and other reptitive tasks.",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "9.6.1"
  },
  "author": "Nuno Reis",
  "license": "ISC",
  "devDependencies": {
    "eslint-config-rallycoding": "^3.2.0",
    "grunt": "^1.0.2",
    "grunt-contrib-cssmin": "^2.2.0",
    "grunt-sass": "^2.0.0"
  },
  "dependencies": {
    "express": "^4.14.0"
  }
}
